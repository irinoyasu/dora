<%= form_with(model: shipper) do |form| %>
  <% if shipper.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shipper.errors.count, "error") %> prohibited this shipper from being saved:</h2>

      <ul>
        <% shipper.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :shipper_id, style: "display: block" %>
    <%= form.text_field :shipper_id %>
  </div>

  <div>
    <%= form.label :company_name, style: "display: block" %>
    <%= form.text_field :company_name %>
  </div>

  <div>
    <%= form.label :contact_info, style: "display: block" %>
    <%= form.text_area :contact_info %>
  </div>

  <div>
    <%= form.label :history_of_shipments, style: "display: block" %>
    <%= form.text_area :history_of_shipments %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
